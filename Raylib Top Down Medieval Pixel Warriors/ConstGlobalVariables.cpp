#pragma once

// Const Global Variables
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
const int SCREEN_WIDTH = 1200;
const int SCREEN_HEIGHT = 800;

const enum ChosenGroup {SPEARMEN, ARCHERS, KNIGHTS};
const enum Status {ATTACK, DEATH, IDLE, RUN};
const enum Command { PLAYER_INPUT, HOLD_POSITION, ENGAGE, COMPLETELY_STOP};
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------